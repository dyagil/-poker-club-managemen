# הוראות פתרון:
# 1. חפש בקובץ app.py את שתי הפונקציות generate_report
# 2. מחק את הפונקציה הראשונה (שורות ~790-815) והשאר רק את הפונקציה השנייה (שורות ~893-911)
# 3. העתק את הטקסט הבא במקום הפונקציה הראשונה שמחקת:

# פונקציה זו הועברה למטה ומוזגה עם generate_report השנייה
# כדי למנוע כפילות endpoint

# תוכל למצוא את שתי הפונקציות generate_report בקובץ app.py באמצעות חיפוש:
# @app.route('/generate_report')
